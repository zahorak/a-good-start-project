= form_for @school_result do |f|
  -if @school_result.errors.any?
    #error_explanation
      %h2= "#{pluralize(@school_result.errors.count, "error")} prohibited this school_result from being saved:"
      %ul
        - @school_result.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
